# 翼智小马项目问题清单

更新时间：2026-06-11

本文档按照产品可用性、安全性、Agent 可靠性和工程质量整理当前待完善事项。优先级说明：

- `P1`：影响安全、稳定性或用户信任，应优先处理。
- `P2`：影响长期使用、维护成本或核心体验，应尽快处理。
- `P3`：体验和工程一致性问题，可排入后续迭代。

## P1：优先处理

### 1. 企业控制台大量设置尚未真实保存

- **现状**：个人资料、企业信息、安全策略、通知、默认页面和界面缩放等表单点击保存后，仅显示“配置已保存”。
- **影响**：修改不会写入数据库或配置文件，重新打开页面或重启应用后丢失，容易误导用户。
- **整改建议**：
  - 明确区分真实设置与演示设置。
  - 为真实设置设计共享类型、IPC 和持久化表。
  - 表单改为受控状态，保存失败时展示明确错误。
- **验收标准**：修改设置后重新打开应用仍保持一致；失败时不得显示保存成功。

### 2. 模型生成报告可执行脚本

- **现状**：报告允许包含 JavaScript，并在 iframe 和 PDF 导出窗口中执行。
- **影响**：模型生成或外部输入的脚本可能发起网络请求、消耗资源或执行非预期行为。
- **整改建议**：
  - 优先改为结构化图表数据，由应用负责生成 ECharts 配置。
  - 为报告文档增加严格 CSP，禁止外部网络、表单和导航。
  - 对 HTML 标签、属性和脚本能力采用白名单。
  - PDF 导出窗口开启隔离和 sandbox。
- **验收标准**：报告无法访问外部网络、主窗口 API、本地文件和任意 Electron 能力。

### 3. Electron 主窗口未开启 sandbox

- **现状**：主窗口设置了 `sandbox: false`。
- **影响**：渲染进程发生脚本注入或依赖漏洞时，潜在影响范围扩大。
- **整改建议**：
  - 开启 `sandbox: true`。
  - 审计 preload 暴露的全部 IPC 方法。
  - 为 IPC 参数增加运行时校验和调用来源校验。
- **验收标准**：开启 sandbox 后首页、控制台、工作台、上传、导出和 Agent 调用均正常。

### 4. 应用初始化失败会永久停留在加载页

- **现状**：初始化 Promise 没有错误捕获、超时和重试入口。
- **影响**：数据库、MCP 或配置读取失败后，用户只能看到“正在准备智能工作空间”。
- **整改建议**：
  - 增加 `loading / ready / error` 初始化状态。
  - 展示可读错误、重试按钮和打开设置入口。
  - 非关键模块失败时允许降级进入应用。
- **验收标准**：模拟任一初始化接口失败时，页面在合理时间内显示错误并可重试。

## P2：核心完善

### 5. 外部链接缺少协议白名单

- **现状**：新窗口 URL 直接交给 `shell.openExternal`。
- **影响**：可能处理非预期协议。
- **整改建议**：仅允许 `https:`，根据产品需要单独允许 `mailto:`，其余协议全部拒绝并记录日志。
- **验收标准**：`file:`、自定义协议及其他非白名单链接无法被打开。

### 6. 咨询与任务共享同一份上下文

- **现状**：直接咨询和发布任务共用聊天历史，运行记录也未保存交互模式。
- **影响**：长期使用后，咨询内容可能干扰任务决策；历史页面无法区分咨询和任务。
- **整改建议**：
  - 为消息和运行记录增加 `mode: chat | task`。
  - 引入会话 ID，支持新建、切换和删除会话。
  - 主 Agent 按会话和模式选择上下文。
- **验收标准**：不同会话互不影响；任务历史可按模式筛选。

### 7. 缺少自动化测试

- **现状**：项目只有类型检查，没有有效测试用例，根目录 `test.py` 为空。
- **影响**：Agent、IPC、数据库和界面改动容易产生回归。
- **整改建议**：
  - 为 SQL 防护、报告清洗、Skill 解析增加单元测试。
  - 为主 Agent 派单规则和模式隔离增加可控模型测试。
  - 为 IPC 增加参数校验测试。
  - 为首页问答、发布任务、主题切换和历史折叠增加组件或端到端测试。
- **验收标准**：CI 自动执行类型检查、单元测试和核心流程测试。

### 8. 全局主题仍依赖大量后置 CSS 覆盖

- **现状**：浅色和深色主题主要通过样式文件末尾的选择器覆盖，原始样式含大量硬编码颜色。
- **影响**：新增组件容易遗漏主题适配，深色模式可能出现低对比度或残留暖色。
- **整改建议**：
  - 建立语义化设计令牌：画布、面板、浮层、文字、边框、强调色和状态色。
  - 逐步清除组件中的硬编码颜色。
  - 为三套主题增加页面截图回归测试。
- **验收标准**：新增组件仅使用设计令牌即可自动适配全部主题。

### 9. IPC 参数缺少统一运行时校验

- **现状**：部分 IPC 仅进行简单类型判断，不同接口校验方式不统一。
- **影响**：异常参数可能进入数据库、文件系统或 Agent 执行层。
- **整改建议**：使用 Zod 为每个 IPC 请求定义 schema，并统一错误结构。
- **验收标准**：非法参数在 IPC 边界被拒绝，渲染层收到可识别错误码和消息。

### 10. Agent 运行状态仅保存在内存

- **现状**：主进程用全局变量记录当前运行任务，应用异常退出后无法恢复。
- **影响**：任务中断后状态可能不一致，用户无法判断是否完成或失败。
- **整改建议**：
  - 在数据库记录运行状态和最后事件。
  - 启动时将未完成任务标记为中断。
  - 支持恢复展示和重新执行。
- **验收标准**：任务执行期间强制退出应用，重启后可看到明确的中断状态。

## P3：体验与维护

### 11. 首页发布任务后立即跳转，失败反馈较弱

- **现状**：调用发送接口后立即进入任务工作台，没有等待主进程确认受理。
- **影响**：IPC 调用失败时，用户可能只看到工作台无响应。
- **整改建议**：发送接口返回“已受理”后再跳转；失败时在首页显示 Toast 并保留输入内容。
- **验收标准**：模拟模型配置错误或 IPC 失败时，页面不跳转且提示明确。

### 12. 原生 alert 和 confirm 与产品体验不统一

- **现状**：上传失败、数据调整、删除报告和解雇小马仍使用系统弹窗。
- **影响**：无法适配主题，也不利于错误归类和自动化测试。
- **整改建议**：建立全局 Toast、确认对话框和错误中心。
- **验收标准**：业务界面不再直接调用 `window.alert` 或 `window.confirm`。

### 13. 首页历史对话仅按进入页面时的消息数量划分

- **现状**：历史折叠以组件挂载时的消息数量为边界，不是真正的会话分组。
- **影响**：切换页面后再次返回首页，本轮消息会被重新归入历史；无法按任务查看完整上下文。
- **整改建议**：引入持久化会话模型，按会话而不是数组位置分组。
- **验收标准**：页面切换不会改变消息所属会话，用户可显式新建对话。

### 14. 首页个人中心部分入口不能直接打开对应设置

- **现状**：个人资料、企业信息和账号安全仅进入企业控制台，没有直接打开对应抽屉。
- **影响**：入口语义与实际行为不完全一致。
- **整改建议**：统一使用带目标面板的导航状态或路由参数。
- **验收标准**：点击个人资料后直接打开个人资料设置抽屉。

### 15. 报告主题没有跟随全局外观

- **现状**：生成报告 HTML 固定使用暖色浅色主题。
- **影响**：深色模式下打开报告会突然变成高亮浅色页面。
- **整改建议**：报告模板根据当前外观生成，或让报告 iframe 接收主题令牌。
- **验收标准**：三种外观下报告都具有合适的对比度和一致风格。

### 16. 日志与可观测性不足

- **现状**：日志主要输出到控制台，缺少统一错误编号、日志文件轮转和用户可导出的诊断包。
- **影响**：生产环境问题难以复现和定位。
- **整改建议**：增加结构化日志、运行 ID、错误码、日志文件轮转和一键导出诊断信息。
- **验收标准**：用户反馈失败任务时，可导出包含配置摘要、运行事件和错误日志的诊断包，且敏感信息已脱敏。

### 17. 构建与发布流程缺少持续集成

- **现状**：没有看到 CI 配置、自动打包验证和版本发布流程。
- **影响**：分支合并后可能出现只有开发环境可运行的问题。
- **整改建议**：增加 GitHub Actions，执行安装、类型检查、测试、构建和安装包产物检查。
- **验收标准**：每个 Pull Request 都有自动检查，主分支可生成带版本号的安装包。

## 推荐实施顺序

1. 报告脚本安全、Electron sandbox、外部链接白名单。
2. 初始化错误页和统一错误处理。
3. 明确企业控制台哪些设置真实可用，并完成持久化。
4. 建立会话模型，隔离咨询与任务上下文。
5. 建立自动化测试和 CI。
6. 重构主题设计令牌并完成报告主题适配。
7. 完善 Toast、确认框、日志和诊断能力。

## 当前验证情况

- TypeScript 类型检查已通过。
- 生产构建曾因审查环境文件系统权限失败，尚未完成安装包级验证。
- 当前未发现有效的自动化测试套件。
