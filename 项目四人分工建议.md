# 翼智小马四人项目分工建议

## 一、项目概述

本项目为 **翼智小马：数字员工解决方案整合供应平台**。当前代码实现形态为 Electron 桌面端应用“小马办公室”，通过 React 管理界面、PixiJS 协作场景、Vercel AI SDK Agent 编排、MCP 工具接入、SQLite 本地存储与 Excel 数据分析链路，实现数字员工接入、任务派单、经营分析、报告生成、Skill 扩展与文件归档等能力。

项目后续目标是从演示级真实闭环，逐步扩展为可复用、可授权、可计量、可交付的数字员工解决方案供应平台，覆盖经营分析、智能营销、调账稽核等首批场景。

## 二、四人分工总览

| 成员 | 角色 | 主要职责 | 核心交付物 |
| --- | --- | --- | --- |
| 成员 1 | 前端与可视化 | 负责 React 管理界面、PixiJS 小马办公室场景、任务日志、报告面板、小马招聘与配置、设置页、动画状态展示 | 可演示 UI、协作动画、报告查看与导出入口、任务状态可视化 |
| 成员 2 | 后端与桌面主进程 | 负责 Electron 主进程、IPC、SQLite 数据、Excel 导入、报告/PDF、MCP Server 托管、文件归档、安全确认框、运行日志 | 稳定的数据链路、工具调用链路、MCP/Skill/小马 CRUD、报告与归档能力 |
| 成员 3 | 算法与 Agent 编排 | 负责领队马调度逻辑、子 Agent prompt、Vercel AI SDK tool-calling、SQL 安全守卫、任务拆解、失败重试、三类场景 Agent 方案 | 经营分析、智能营销、调账稽核的 Agent 流程、可观测派单、评测用例 |
| 成员 4 | 产品设计与交付 | 负责产品定位、需求拆解、演示脚本、信息架构、交互原型、视觉规范、验收标准、商业化表达、汇报材料 | PRD/演示稿、用户流程、原型图、比赛/路演脚本、验收清单 |

## 三、成员 1：前端与可视化

### 主要职责

- 负责桌面端用户界面的整体实现与交互体验。
- 维护小马办公室的 PixiJS 场景，包括小马角色、工位、白板、气泡、行走、工作、道歉、报告上墙等动画。
- 维护 React UI 面板，包括对话输入、任务日志、报告面板、小马卡片、招聘表单、MCP/Skill 设置页。
- 根据 Agent 事件流 `agent:event` 驱动画面变化，确保动画和任务日志表达一致。
- 优化演示效果，保证界面清晰、状态可见、汇报时一眼能看懂。

### 重点代码目录

- `src/renderer/src/App.tsx`
- `src/renderer/src/ui/`
- `src/renderer/src/scene/`
- `src/renderer/src/store/appStore.ts`
- `src/renderer/src/theme.css`

### 阶段交付

- 完成主界面、任务输入、上传数据、任务日志、报告查看等基础操作。
- 完成小马点击、招聘、换肤、Skill/MCP 绑定等交互。
- 完成任务运行期间的小马协作动画与状态展示。
- 保证窗口缩放、报告弹窗、右侧日志栏、底部输入栏布局稳定。

## 四、成员 2：后端与桌面主进程

### 主要职责

- 负责 Electron 主进程能力建设，保证渲染进程不直接访问 Node 能力。
- 维护 IPC 接口，确保前端与主进程之间的通信稳定、类型清晰。
- 负责 SQLite 本地数据存储，包括小马、Skill、MCP、对话、报告、运行事件、上传数据表等。
- 负责 Excel 导入与数据表创建，支撑经营分析场景。
- 负责报告 HTML 包装、ECharts 内联、PDF 导出、报告归档。
- 负责 MCP Server 托管、连接测试、工具发现、工具调用日志、安全确认框。
- 负责本地工作区、文件归档、应用退出时 MCP 子进程清理。

### 重点代码目录

- `src/main/ipc.ts`
- `src/main/db/`
- `src/main/mcp/`
- `src/main/reports.ts`
- `src/main/skills/`
- `src/main/workspace.ts`
- `src/shared/types.ts`
- `src/shared/ipc.ts`

### 阶段交付

- 保证 `chat:send`、`file:uploadXlsx`、`report:*`、`pony:*`、`skill:*`、`mcp:*` 等 IPC 通道稳定可用。
- 完成 Excel 到 SQLite 的真实数据链路。
- 完成 MCP 配置、测试、调用、日志与失败处理。
- 完成报告生成、PDF 导出、HTML 归档到工作区。
- 保证 API Key、MCP 凭据、文件操作等安全边界清晰。

## 五、成员 3：算法与 Agent 编排

### 主要职责

- 负责多 Agent 协作逻辑与任务派单策略。
- 维护领队马、数据马、报表马、文书马、文件马及自定义小马的 system prompt。
- 负责 Vercel AI SDK 的 `streamText`、`generateText`、tool-calling 循环、`dispatch` 工具设计。
- 负责经营分析链路：自然语言问题识别、SQL 生成、SQL 安全守卫、结果解读、报告数据传递。
- 负责失败重试、低置信度提示、任务失败后的如实反馈。
- 设计智能营销、调账稽核两个场景的 Agent 流程与演示样例。
- 建立基础评测用例，验证派单是否真实发生、SQL 是否安全、报告数据是否不编造。

### 重点代码目录

- `src/main/agents/index.ts`
- `src/main/agents/prompts.ts`
- `src/main/agents/sqlGuard.ts`
- `src/main/llm.ts`
- `skills/`

### 阶段交付

- 完成领队马自动派单与多小马串联执行。
- 完成数据马 text-to-SQL、只读 SQL 守卫与失败重试。
- 完成报表马基于真实分析数据生成 ECharts 报告。
- 完成文件马归档、文书马总结、自定义小马接单等 prompt 策略。
- 输出三类首批方案的 Agent 流程说明：经营分析、智能营销、调账稽核。

## 六、成员 4：产品设计与交付

### 主要职责

- 负责项目定位、目标用户、核心价值、业务流程和商业化表达。
- 将立项书、需求文档和当前代码能力整合成统一产品叙事。
- 负责演示脚本设计，保证汇报时能清楚展示“数字员工接入、派单、协作、工具调用、报告生成、方案交付”。
- 负责信息架构和交互原型，指导前端页面、面板、状态和文案设计。
- 负责验收标准和测试清单，组织阶段性验收。
- 负责路演 PPT、讲稿、产品说明、交付清单、商业模式说明。

### 重点文档

- `prd.md`
- `docs/接口与实施规范.md`
- `docs/M3接口与实施规范.md`
- `解决方案供应版项目立项书(3).md`
- `解决方案供应版需求文档(2).md`

### 阶段交付

- 完成产品定位与用户场景说明。
- 完成项目汇报材料与演示流程。
- 完成三类解决方案的业务脚本：经营分析、智能营销、调账稽核。
- 完成验收清单与分阶段里程碑。
- 完成面向内部试点和 To B 商业化的价值表达。

## 七、协作边界

- 前后端接口以 `src/shared/types.ts` 和 `src/shared/ipc.ts` 为唯一契约。
- 前端只消费 `agent:event` 事件流，不承载核心调度逻辑。
- 后端负责数据、文件、MCP、报告、PDF、IPC，不直接处理界面表现。
- 算法只通过工具、MCP、SQLite 查询接口拿数据，不直接改 UI。
- 产品负责把需求拆成可开发、可演示、可验收的闭环任务，避免范围发散。
- 高风险能力，如文件覆盖、删除、流程退回、外部系统写操作，必须有规则授权或人工确认。

## 八、推荐里程碑

| 阶段 | 目标 | 主要负责人 | 验收标准 |
| --- | --- | --- | --- |
| M1 基础演示 | 启动应用、显示办公室、小马动画、基础对话入口 | 前端 | 应用可启动，主界面稳定，小马动画正常 |
| M2 数据闭环 | 上传 Excel、数据入库、SQL 分析、生成报告 | 后端 + 算法 | 经营分析端到端跑通，报告可查看 |
| M3 扩展能力 | 小马招聘、Skill、MCP、文件马归档 | 前端 + 后端 | 自定义小马可接单，MCP 可测试，报告可归档 |
| M4 场景方案 | 经营分析、智能营销、调账稽核三类方案打磨 | 算法 + 产品 | 三类演示脚本可跑通或可清晰讲解 |
| M5 汇报交付 | 路演材料、验收清单、商业模式包装 | 产品 | 完整演示彩排通过，材料可提交 |

## 九、最终验收建议

- 能上传电信业务 Excel，并完成一次自然语言经营分析。
- 任务日志能展示派单、SQL、工具调用、报告生成等关键过程。
- PixiJS 场景能同步展示小马协作状态。
- 报告能在白板或报告面板查看，并支持导出或归档。
- 能招聘一只自定义小马，并通过 Skill/MCP 扩展能力。
- SQL 守卫能拒绝非只读操作。
- 文件覆盖、删除等高风险操作有人工确认。
- 立项价值、需求目标、技术架构、演示流程、商业模式表达一致。
