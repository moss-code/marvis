# 开发计划

## 1. 项目框架

- 工具形态：桌面软件
- 主框架：Electron
- 当前技术栈：Electron + Vite + React + TypeScript + PixiJS v8 + Vercel AI SDK + MCP + SQLite + SheetJS + ECharts
- 框架映射：桌面软件 -> Electron；主进程负责本地可信执行边界，渲染进程只负责 UI 展示
- 选择理由：PRD 明确需要本地文件、SQLite、MCP stdio 子进程、PDF 导出和真实数据分析闭环，Electron 比 Web 后端更贴合单机演示产品。
- 规划来源：`prd.md`、现有代码结构、`02-project-prepare` 多 Agent 拆解结果。
- Agent 启用矩阵：
  - 必选：mvp-requirement-analyst、project-architecture-planner、qa-acceptance-planner
  - 条件启用：ui-interaction-planner（桌面应用有 Pixi 场景与 React 面板）；api-integration-planner（LLM、MCP、SheetJS、SQLite、ECharts、PDF）；platform-capability-planner（Electron、本地文件、MCP 子进程、工作区权限）
  - 未启用：无
- 当前代码观察（不等于已验收完成）：
  - 已有 `src/main`、`src/preload`、`src/renderer`、`src/shared` 分层。
  - 已有 `AgentEvent`、领队马派单、SQL guard、xlsx 导入、报告/PDF、MCP 管理、Pixi 场景、招聘表单、设置面板等基础代码。
  - `package.json` 的 `dev` 当前为 `electron-vite dev`，后续需按 Electron 开发刷新要求确认是否改为 `electron-vite dev --watch` 或在 `electron.vite.config.ts` 配置 main/preload watch。

## 已完成模块（详见 prd/）

> 首版留空。某 §2.x 功能全部 `[x]` 且经 project-verify 验证后，按 `.claude/rules/todo-prd-archive.md` 写入对应 `prd/<模块>/README.md` 和 `prd/<模块>/technical.md`，并在此追加索引；§2 中该块折叠为「已归档 -> prd/<模块>/README.md」。

（暂无）

## 2. MVP 功能拆解

### 2.1 办公室与小马阵容初始化

- 用户目标：启动后立即看到一间可演示的 2D 办公室，理解 5 只预置小马、1 个空工位和白板报告入口。
- 使用入口：应用启动后的主界面。
- 子功能：
  - [ ] 读取 SQLite 中的小马配置；首次启动自动初始化领队马、数据马、报表马、文件马、文书马。
  - [ ] 渲染 6 个横向工位、白板、绿植、吊灯、窗光等基础办公室元素。
  - [ ] 让至少 5 只预置小马具备 idle 呼吸/眨眼，空工位可点击进入招聘流程。
  - [ ] 点击小马打开配置面板；点击白板在无报告时展示空状态，在有报告时打开报告面板。
- 主流程：
  1. 应用启动并初始化数据库。
  2. 主进程读取或创建小马配置。
  3. 渲染进程加载办公室场景和 React overlay。
  4. 小马进入 idle 状态，输入栏可用。
- 状态要求：
  - 默认态：5 只预置小马 + 1 个空工位 + 白板完整可见。
  - 空态：无数据库时自动创建默认阵容，不显示空白页。
  - 加载态：显示轻量场景骨架或“正在布置办公室”。
  - 成功态：小马可点击、空工位可招聘、白板可查看报告。
  - 错误态：配置读取失败时恢复默认阵容，并在日志显示原因。
- 数据需求：`Pony`、`PonySkin`、Skill 绑定、MCP 绑定、报告列表。
- 数据来源：`LIVE`；首次默认阵容为 `FIXTURE`。
- 演示策略：`LIVE + FIXTURE_DEMO`，默认阵容可直接演示，但必须来自真实本地状态。
- 首屏可演示形态：办公室场景、小马 idle、白板空状态、底部输入栏。
- MVP 边界：只支持一个办公室；小马总数上限 6。
- 暂不实现：多办公室、自由拖动工位、云端同步、3D 场景。
- 验收标准：
  - [ ] 应用启动后 1280 x 800 下场景、输入栏、日志、白板不互相遮挡。
  - [ ] Pixi canvas 非空，3 秒内存在动画帧变化。
  - [ ] 点击预置小马打开配置面板，点击空工位打开招聘表单。

### 2.2 领队马对话入口与 xlsx 上传

- 用户目标：用户只需要在底部输入栏发任务并上传 xlsx，就能触发领队马编排。
- 使用入口：底部固定输入栏、附件按钮、拖拽上传。
- 子功能：
  - [ ] 输入框支持文字、多行输入、发送按钮、任务中禁用重复提交。
  - [ ] 附件按钮支持 `.xlsx`，展示文件名、大小、移除按钮。
  - [ ] 非 xlsx、损坏 xlsx、空表格、文件过大时给出明确错误。
  - [ ] 上传成功后任务日志展示文件名、sheet 数、行列摘要。
- 主流程：
  1. 用户上传或拖入电信业务 xlsx。
  2. 用户输入“分析各营业厅业务表现并出一份报告”。
  3. 渲染进程通过 preload 调用主进程 IPC。
  4. 主进程解析文件并创建任务运行记录。
  5. 领队马进入思考和派单流程。
- 状态要求：
  - 默认态：输入栏可编辑，附件按钮可用。
  - 空态：placeholder 提示上传电信数据并提问。
  - 加载态：显示“领队马理解中 / 正在解析表格”。
  - 成功态：任务进入派单流程。
  - 错误态：模型未配置、文件不合规、任务为空时阻止发送。
- 数据需求：用户消息、附件 metadata、上传文件路径、导入结果、任务状态。
- 数据来源：用户上传 xlsx 为 `LIVE`；内置演示 xlsx 为 `FIXTURE_DEMO`。
- 演示策略：`LIVE + FIXTURE_DEMO`；示例入口必须标注“示例数据”。
- FIXTURE 说明：演示数据由 `npm run make-demo` 生成到 `assets/demo/`，禁止冒充用户上传。
- 首屏可演示形态：底部输入栏 + 附件 chip + 上传成功摘要。
- MVP 边界：MVP 仅支持 `.xlsx`，单任务串行。
- 暂不实现：多会话标签、语音输入、直接私聊其他小马。
- 验收标准：
  - [ ] 空文本且无附件时发送按钮禁用。
  - [ ] 上传 `.txt` 或损坏 xlsx 时不进入 LLM 分析。
  - [ ] 任务运行中重复发送不会创建第二个任务。

### 2.3 领队马派单与 Agent 执行流

- 用户目标：用户不需要理解底层流程，也能看到领队马自动拆解任务并派给职能小马。
- 使用入口：用户发送任务后自动触发。
- 子功能：
  - [ ] 领队马读取用户消息、附件、可用小马花名册和 Skill/MCP 能力。
  - [ ] `dispatch(ponyId, taskBrief)` 派单工具强制产生 `task_dispatched` 事件。
  - [ ] 子任务结果回传领队马，领队马决定是否继续派单。
  - [ ] 领队马最终回复只基于子任务真实结果，不虚构。
- 主流程：
  1. 领队马理解用户意图。
  2. 对数据分析任务先派给数据马。
  3. 数据马完成后把结果交给领队马。
  4. 领队马再派给报表马生成报告。
  5. 必要时派给文件马归档或文书马总结。
  6. 领队马汇总并回复用户。
- 状态要求：
  - 默认态：领队马待命。
  - 空态：意图不明确时追问用户。
  - 加载态：显示“理解任务 / 派给数据马 / 报表马整理中”。
  - 成功态：所有必要子任务完成，领队马汇报 artifact。
  - 错误态：目标小马不存在、工具不可用、模型未配置时透明失败。
- 数据需求：`Pony`、`Skill`、`McpServerConfig`、`AgentEvent`、任务运行记录、对话历史。
- 数据来源：`LIVE`
- 演示策略：`LIVE_UNCONFIGURED`；模型未配置时提示配置，不生成假答案。
- 首屏可演示形态：发送任务后领队马思考并产生派单日志。
- MVP 边界：核心路径必须覆盖“领队马 -> 数据马 -> 报表马 -> 领队马”。
- 暂不实现：复杂 DAG 编辑器、多领队、长期后台任务。
- 验收标准：
  - [ ] 典型数据分析任务至少产生数据马和报表马两次派单。
  - [ ] 任务日志能看到派单对象、摘要、开始/结束、状态。
  - [ ] 若领队马未实际派单，系统不得汇报已完成分析。

### 2.4 xlsx -> SQLite 数据导入

- 用户目标：上传表格后，系统把业务数据转换为数据马可查询的 SQLite 表。
- 使用入口：附件上传后由主进程自动执行。
- 子功能：
  - [ ] 校验扩展名、文件大小、可读性。
  - [ ] SheetJS 解析 workbook 和每个 sheet。
  - [ ] 为每个 sheet 生成安全表名和列名映射。
  - [ ] 写入 SQLite，并记录 schema、rowCount、sampleRows。
  - [ ] 空 sheet、全空列、重复列、特殊字符列给出清洗策略和日志。
- 主流程：
  1. 主进程收到 xlsx 文件。
  2. 复制或读取受控路径。
  3. SheetJS 解析 sheet。
  4. SQLite 建表并写入数据。
  5. 生成 schema + 每表 5 行样例。
  6. 向渲染进程返回数据集摘要。
- 状态要求：
  - 默认态：无数据集或已有数据集。
  - 空态：用户只发文本时提示“请先上传 xlsx”。
  - 加载态：解析中、入库中、生成样例中。
  - 成功态：显示 sheet 数、总行数、表名摘要。
  - 错误态：空表、表头缺失、损坏文件、SQLite 写入失败。
- 数据需求：SQLite 数据表、表结构、原始列名映射、样例行、上传 metadata。
- 数据来源：`LIVE`
- 演示策略：`LIVE + FIXTURE_DEMO`
- FIXTURE 说明：`assets/demo/电信业务数据.xlsx` 用于培训彩排。
- 首屏可演示形态：上传后显示数据准备完成和表摘要。
- MVP 边界：支持一个 xlsx 多 sheet；每次上传生成新 dataset，不覆盖旧数据。
- 暂不实现：csv/ods、流式超大文件、手动字段映射 UI。
- 验收标准：
  - [ ] 每个有效 sheet 入库为一张可查询表。
  - [ ] schema 中包含列名、类型、行数和 5 行样例。
  - [ ] 入库失败不留下半成品表误导后续分析。

### 2.5 数据马 text-to-SQL 分析

- 用户目标：数据马根据自然语言任务写 SELECT SQL、执行查询并解释业务含义。
- 使用入口：领队马派单自动触发。
- 子功能：
  - [ ] 读取 dataset schema 和每表样例行。
  - [ ] 生成只读 SQL。
  - [ ] 执行前调用 SQL guard，拒绝 DDL/DML、多语句、PRAGMA/ATTACH 等危险语句。
  - [ ] SQL 失败后把错误回喂数据马，最多重试 2 次。
  - [ ] 输出 SQL、结果摘要、业务结论、可视化建议。
- 主流程：
  1. 数据马收到任务 brief。
  2. 获取 schema/sampleRows。
  3. LLM 生成 SQL。
  4. 主进程校验并执行 SQL。
  5. 数据马解读结果。
  6. 结果回传领队马。
- 状态要求：
  - 默认态：数据马待命。
  - 空态：无 dataset 时提示上传 xlsx。
  - 加载态：生成 SQL、执行 SQL、解读结果分阶段展示。
  - 成功态：输出可转交报表马的结构化分析。
  - 错误态：非 SELECT、SQL 错误、结果为空、模型超时。
- 数据需求：schema/sampleRows、SQLite 查询结果、SQL 尝试记录、任务事件。
- 数据来源：`LIVE`
- 演示策略：`LIVE_UNCONFIGURED`；模型未配置或数据缺失时不生成假结果。
- 首屏可演示形态：日志显示 SQL 和返回行数。
- MVP 边界：只允许 SELECT/WITH SELECT；结果行数上限建议 200。
- 暂不实现：写入型 SQL、用户 SQL 编辑器、多数据集跨库 join。
- 验收标准：
  - [ ] `DROP`、`DELETE`、`INSERT`、`UPDATE`、多语句必须被拒绝。
  - [ ] SQL 失败最多重试 2 次，并记录每次错误。
  - [ ] 成功结果包含 SQL、结果摘要、业务解释和报表建议。

### 2.6 报表马 ECharts 报告与报告面板

- 用户目标：用户获得一份应用内可读、可展示、可导出的数据分析报告。
- 使用入口：数据马分析完成后由领队马派给报表马；白板或日志打开报告面板。
- 子功能：
  - [ ] 生成报告标题、摘要、关键指标、表格、图表建议。
  - [ ] 生成或校验 ECharts 配置。
  - [ ] 主进程生成安全 HTML 报告，ECharts 使用本地资源。
  - [ ] 白板显示报告已生成状态。
  - [ ] 报告面板支持查看、切换历史报告、导出 PDF。
- 主流程：
  1. 报表马收到分析结论和结果集。
  2. 生成报告结构。
  3. 主进程渲染 HTML 并保存 report artifact。
  4. 事件流通知白板高亮。
  5. 用户点击白板打开报告面板。
- 状态要求：
  - 默认态：无报告时白板显示空状态。
  - 空态：无分析数据时不生成报告。
  - 加载态：显示“正在整理报告 / 正在生成图表”。
  - 成功态：报告面板展示标题、摘要、图表和建议。
  - 错误态：图表配置无效、HTML 渲染失败、报告不存在。
- 数据需求：分析结果、chart config、report html、report metadata。
- 数据来源：`LIVE`
- 演示策略：`LIVE`
- 首屏可演示形态：白板报告入口 + 报告面板。
- MVP 边界：至少柱状图、折线图、表格摘要；报告默认标题“电信营业厅业务表现分析报告”。
- 暂不实现：拖拽式 BI、多页仪表盘、在线分享。
- 验收标准：
  - [ ] 报告至少包含标题、摘要、关键指标、1 个 ECharts 图表、建议。
  - [ ] 报告内容来自本轮真实分析结果。
  - [ ] 报告 HTML 不加载远程脚本，不执行不可信 JS。

### 2.7 协作可视化与任务日志

- 用户目标：培训听众能看懂多 Agent 协作过程，演示者能展开日志证明真实执行。
- 使用入口：任务运行期间自动展示；右侧任务日志可展开/收起。
- 子功能：
  - [ ] `task_dispatched` 驱动领队马行走与派单气泡。
  - [ ] `tool_call_started` 驱动小马工作动画和进度指示。
  - [ ] `tool_call_finished` 更新日志和进度。
  - [ ] `task_completed` 触发成果传递动画。
  - [ ] `task_failed` 触发挠头道歉动画和失败日志。
  - [ ] 日志默认紧凑时间线，SQL/JSON 可展开。
- 主流程：
  1. 主进程产生 AgentEvent。
  2. IPC 推送渲染进程。
  3. Pixi 场景消费事件播放动画。
  4. React 日志消费同一事件追加条目。
  5. 任务完成或失败后状态一致收束。
- 状态要求：
  - 默认态：日志为空或显示最近任务。
  - 空态：无任务记录时提示“暂无任务记录”。
  - 加载态：当前步骤高亮，日志自动滚动到底。
  - 成功态：任务节点标记完成，白板报告可点。
  - 错误态：失败节点使用赤陶色，展示用户友好原因和技术详情。
- 数据需求：`AgentEvent`、任务日志、SQL/tool 摘要、错误摘要。
- 数据来源：`LIVE`
- 演示策略：`LIVE`
- 首屏可演示形态：右侧可收起日志 + 场景同步动画。
- MVP 边界：动画只绑定关键事件，细节由日志承载。
- 暂不实现：完整时间轴回放、自定义动画编辑。
- 验收标准：
  - [ ] 动画和日志消费同一主进程事件源，不在 renderer 伪造任务进度。
  - [ ] 日志包含派单、SQL、工具调用、结果摘要、错误信息。
  - [ ] 事件顺序错乱时日志仍按时间排序，动画失败不影响任务继续。

### 2.8 失败重试与优雅认错

- 用户目标：演示中即使模型、SQL、MCP、工具失败，应用也不崩溃、不造假，并解释发生了什么。
- 使用入口：所有任务流程内自动触发。
- 子功能：
  - [ ] SQL/工具失败最多自动重试 2 次。
  - [ ] LLM 超时、断网、MCP 子进程退出进入真实失败提示。
  - [ ] 重试耗尽后触发小马道歉动画和领队马失败总结。
  - [ ] 不使用隐藏成功脚本兜底。
- 主流程：
  1. 工具或 SQL 执行失败。
  2. 判断是否可重试。
  3. 把错误回喂对应小马。
  4. 重试成功则恢复流程。
  5. 重试耗尽则停止该分支并向用户说明。
- 状态要求：
  - 默认态：任务正常。
  - 空态：无错误。
  - 加载态：显示“正在尝试修正”。
  - 成功态：重试后继续后续派单。
  - 错误态：明确原因和下一步建议。
- 数据需求：error event、retry attempts、final failure message。
- 数据来源：`LIVE`
- 演示策略：`LIVE`；失败演示必须是真实失败触发。
- 首屏可演示形态：失败日志 + 道歉气泡 + 输入栏恢复。
- MVP 边界：SQL 和工具各最多重试 2 次；LLM 超时建议 60 秒；工具超时建议 30 秒。
- 暂不实现：自动切换模型供应商、云端错误上报。
- 验收标准：
  - [ ] 断网/API 超时应用不崩溃。
  - [ ] 重试次数、失败原因、最终建议都进入任务日志。
  - [ ] 重试耗尽后不得生成假报告。

### 2.9 招聘新小马、皮肤和配置

- 用户目标：现场创建一只新职能小马，展示系统可扩展性。
- 使用入口：点击空工位。
- 子功能：
  - [ ] 招聘表单包含名称、职能描述、调色板、配件、Skill 勾选、MCP server 勾选。
  - [ ] 调色板限定亚麻白、驼色、赭石、鼠尾草绿、赤陶。
  - [ ] 保存后写入 SQLite，`builtin=false`。
  - [ ] 新小马播放入场动画，并出现在领队马花名册。
  - [ ] 预置小马不可删除，但可换皮肤、追加 Skill、绑定 MCP。
- 主流程：
  1. 点击空工位。
  2. 填写基本信息。
  3. 选择皮肤和配件。
  4. 勾选 Skill/MCP。
  5. 保存并入驻。
- 状态要求：
  - 默认态：空工位可点击。
  - 空态：名称/职责为空时保存禁用。
  - 加载态：保存中。
  - 成功态：新小马显示 idle，并可被派单。
  - 错误态：名称重复、超过 6 只、MCP 不可用、保存失败。
- 数据需求：`PonyDraft`、Skill 列表、MCP 列表、皮肤 token。
- 数据来源：`LIVE`
- 演示策略：`LIVE`
- 首屏可演示形态：招聘表单 + 入场动画。
- MVP 边界：只支持 1 只自定义小马；自定义小马可删除，预置不可删除。
- 暂不实现：多只自定义小马、小马市场、复杂权限控制。
- 验收标准：
  - [ ] 办公室总数不超过 6。
  - [ ] 新小马保存后立即进入可派单列表。
  - [ ] 任务运行中修改小马配置需要禁用或提示稍后修改。

### 2.10 Skill 库与 MCP 配置

- 用户目标：通过 Markdown Skill 和 MCP 工具扩展小马能力，而不是改代码。
- 使用入口：设置页、小马配置页。
- 子功能：
  - [ ] 预置 Skill 库，支持自定义 Skill 新建/编辑/保存。
  - [ ] 小马可勾选 Skill，后续执行任务时注入 system prompt。
  - [ ] 全局 MCP server 可配置 name/command/args/env/enabled。
  - [ ] 主进程以 stdio 托管 MCP server，自动发现工具。
  - [ ] 小马可勾选绑定 MCP server，工具集按绑定过滤。
  - [ ] env 中疑似 secret 的值默认 mask，日志脱敏。
- 主流程：
  1. 用户打开设置或小马配置。
  2. 新建/编辑 Skill 或 MCP server。
  3. 在小马配置中勾选能力。
  4. 保存绑定。
  5. 后续任务使用新的 prompt/tool 集。
- 状态要求：
  - 默认态：显示预置 Skill 与 filesystem MCP。
  - 空态：无自定义 Skill/MCP 时显示创建入口。
  - 加载态：MCP 启动和工具发现中。
  - 成功态：显示工具数量和可用状态。
  - 错误态：命令不存在、启动失败、env 缺失、markdown 为空。
- 数据需求：Skill markdown、MCP server spec、绑定关系、工具发现结果。
- 数据来源：Skill 预置为 `FIXTURE`，用户配置为 `LIVE`。
- 演示策略：`LIVE_UNCONFIGURED`；MCP 不可用时提示配置，不伪装可用。
- 首屏可演示形态：设置页显示 filesystem MCP 与 Skill 列表。
- MVP 边界：filesystem MCP 限定工作区；MCP 懒加载，首次使用时启动。
- 暂不实现：插件市场、远程 MCP 托管、Secret 云同步、Skill 版本管理。
- 验收标准：
  - [ ] renderer 不能直接启动/调用 MCP。
  - [ ] MCP 工具发现结果只通过主进程安全代理暴露。
  - [ ] 删除正在被使用的 Skill/MCP 需要确认或阻止。

### 2.11 文件马归档与 PDF 导出

- 用户目标：把报告导出 PDF，并让文件马归档到办公室工作区。
- 使用入口：报告面板导出按钮；对话中要求“归档报告”。
- 子功能：
  - [ ] 报告面板提供导出 PDF。
  - [ ] 主进程使用 Electron `printToPDF`。
  - [ ] 文件马把 HTML/PDF 归档到工作区目录。
  - [ ] 覆盖、删除、越界路径必须确认或拒绝。
  - [ ] 成功后展示保存路径，并记录归档事件。
- 主流程：
  1. 用户打开报告。
  2. 点击导出 PDF 或让领队马派文件马归档。
  3. 主进程生成 PDF。
  4. 文件马写入工作区。
  5. 若文件已存在，弹确认。
  6. 日志展示归档结果。
- 状态要求：
  - 默认态：报告存在时导出按钮可用。
  - 空态：无报告时导出禁用。
  - 加载态：导出中/归档中。
  - 成功态：显示保存路径。
  - 错误态：文件占用、权限不足、PDF 生成失败、工作区越界。
- 数据需求：report id、report html、workspace root、archive record。
- 数据来源：`LIVE`
- 演示策略：`LIVE`
- 首屏可演示形态：报告面板导出按钮 + 归档结果 toast/日志。
- MVP 边界：自动归档只能写入工作区；另存为由用户通过系统对话框选择。
- 暂不实现：云盘同步、真实邮件发送、任意系统目录静默写入。
- 验收标准：
  - [ ] 导出的 PDF 文件存在且大小大于 0。
  - [ ] 工作区外路径、`../`、符号链接逃逸必须被拒绝。
  - [ ] 覆盖已有文件前必须确认。

### 2.12 文书马总结与邮件草稿

- 用户目标：基于最近一次分析结果生成工作总结或邮件草稿，但不真实发送。
- 使用入口：对话输入，例如“让文书马写一份工作总结邮件草稿”。
- 子功能：
  - [ ] 领队马判断文书任务并派给文书马。
  - [ ] 文书马读取最近分析结论或报告摘要。
  - [ ] 输出 Markdown 草稿，明确标注“草稿”。
  - [ ] 可复制或交给文件马归档为文本/Markdown。
- 主流程：
  1. 用户提出文书任务。
  2. 领队马派给文书马。
  3. 文书马读取上下文。
  4. 生成总结/邮件草稿。
  5. 领队马展示结果。
- 状态要求：
  - 默认态：文书马待命。
  - 空态：无分析结果时请用户先完成分析或提供材料。
  - 加载态：生成草稿中。
  - 成功态：展示可复制的 Markdown 草稿。
  - 错误态：上下文缺失、模型超时。
- 数据需求：最近分析结果、报告摘要、对话历史、草稿 artifact。
- 数据来源：`LIVE`
- 演示策略：`LIVE_UNCONFIGURED`
- 首屏可演示形态：对话中展示草稿。
- MVP 边界：只生成草稿，不执行 SMTP 或真实邮件 API。
- 暂不实现：Word/PPT 自动排版、多版本审阅、真实邮件发送。
- 验收标准：
  - [ ] 没有分析结果时文书马不编造上下文。
  - [ ] 用户要求真实发送邮件时必须拒绝并说明当前只支持草稿。
  - [ ] 草稿输出含主题、正文和行动建议。

### 2.13 安全审批与权限治理中心

- 用户目标：当 Agent、MCP、文件马或 Skill script 请求高风险操作时，用户能在执行前看见原因、允许或拒绝，并在事后追溯完整审计记录。
- 使用入口：全局「治理中心」入口、审批弹窗、任务日志中的审批节点、小马配置页的权限策略区。
- 子功能：
  - [x] 主进程增加审批请求模型：`approvalId`、`runId`、`taskId`、`ponyId`、`toolName`、`resource`、`argsSummary`、`riskLevel`、`reason`、`createdAt`、`status`。
  - [x] 高风险工具调用不直接执行，由主进程发起 `approval_required` 或等价 IPC 事件。
  - [x] renderer 展示审批弹窗和审批队列，可允许/拒绝；拒绝后作为正常失败路径回传 Agent。
  - [x] 主进程生成一次性 `approvalToken`，绑定审批请求、动作类型、工具名、资源目标、过期时间；执行前校验并消费 token。
  - [x] 按数字员工配置权限策略：可读文件、可写文件、可调用 MCP、可运行 Skill script、可导出/归档报告。
  - [x] 支持策略等级：只读、工作区写入、需审批写入、禁止危险操作。
  - [x] 工具调用风控预检查：工作区外路径拦截、`../` 拦截、符号链接逃逸拦截、覆盖/删除/移动审批、Skill script 审批、敏感 env 读取直接拒绝。
  - [x] 审计日志记录每次审批与高风险拦截：时间、发起小马、工具、参数摘要、资源目标、风险等级、用户决策、最终结果。
  - [x] 治理中心支持查看审批历史、筛选风险等级/小马/工具、复制脱敏摘要。
- 主流程：
  1. 小马发起工具调用。
  2. 主进程根据工具类型、参数、目标资源和小马权限策略执行风险预检查。
  3. 低风险且授权范围内的操作直接执行并写审计摘要。
  4. 需审批操作生成审批请求，暂停该工具调用。
  5. renderer 展示审批弹窗或队列条目。
  6. 用户允许时 renderer 回传一次性 token；主进程校验 token 后执行动作。
  7. 用户拒绝、token 过期或校验失败时，工具调用以可解释失败返回。
  8. 审计日志记录最终决策和执行结果，任务日志同步展示。
- 状态要求：
  - 默认态：无待审批项，治理中心显示最近审计记录。
  - 空态：尚无审计记录时说明系统会在高风险操作前请求确认。
  - 待审批态：显示发起小马、工具、资源目标、风险等级、原因、允许/拒绝按钮。
  - 成功态：允许后动作执行完成，审计日志标记 `allowed_executed`。
  - 拒绝态：拒绝后 Agent 收到用户拒绝原因，审计日志标记 `denied`。
  - 错误态：token 过期、目标变化、权限不足、路径越界、敏感 env 访问等给出明确原因。
- 数据需求：`ApprovalRequest`、`ApprovalDecision`、`PermissionPolicy`、`AuditLogEntry`、一次性 token 状态、策略绑定关系。
- 数据来源：`LIVE`
- 演示策略：`LIVE`；审批演示必须由真实高风险动作触发，禁止预制审批成功记录冒充。
- 首屏可演示形态：设置或标题栏有「治理中心」入口；文件覆盖/删除或 Skill script 执行时弹出审批。
- MVP 边界：
  - 先覆盖文件马、filesystem MCP 写/改/删/移动、Skill script 执行、报告归档/导出。
  - 权限策略先按小马粒度配置，不做多用户 RBAC。
  - 审计日志先保存在本地 SQLite，不做云端上报。
- 暂不实现：多用户审批流、审批人角色、远程审计平台、复杂策略 DSL、长期授权。
- 验收标准：
  - [ ] 覆盖、删除、移动文件必须进入审批队列；拒绝后文件不变化，Agent 如实汇报。
  - [x] 工作区外路径、`../`、符号链接逃逸、敏感 env 读取必须直接拒绝，且写入审计日志。
  - [x] 同一个 `approvalToken` 只能使用一次；过期、目标不匹配或重复使用必须失败。
  - [x] 自定义小马未授权时不能调用 MCP 或运行 Skill script。
  - [x] 审计日志脱敏展示，不泄露 API key、token、password、Authorization、MCP env 明文。
  - [x] 任务日志和治理中心对同一次审批的状态一致。

## 3. 界面与交互规格

- [ ] 主界面分为 Pixi 场景层和 React UI overlay：场景层负责办公室、小马、气泡、动画、白板；UI 层负责输入、日志、报告、配置、招聘、设置。
- [ ] 底部输入栏固定可见，包含附件按钮、输入框、发送按钮、附件 chip、当前任务短状态；`Enter` 发送，`Shift+Enter` 换行。
- [ ] 右侧任务日志为紧凑时间线，默认可展开/收起；SQL 与 JSON 默认折叠，可复制。
- [ ] 报告面板通过白板或日志入口打开，宽度 60%-72%，最小 720px，最大 1100px；顶部含导出 PDF 和归档按钮。
- [ ] 小马配置页展示名称、职能、状态、皮肤预览、Skill、MCP、内置工具；预置小马核心 role 和 builtinTools 只读。
- [x] 小马配置页增加权限策略区：文件读写、MCP、Skill script、报告导出/归档权限和策略等级。
- [ ] 招聘表单建议 4 步：基本信息、皮肤、Skill、MCP；保存成功后播放入场动画。
- [ ] 设置页包含模型配置和 MCP 配置；API key 默认隐藏，MCP env 中 secret 类字段默认 mask。
- [x] 设置页或标题栏提供「治理中心」入口，展示待审批队列、审批历史和审计日志。
- [ ] 审批弹窗必须清楚展示发起小马、工具名、资源目标、风险等级、原因、参数摘要、允许/拒绝按钮。
- [ ] 动画映射：
  - `task_dispatched` -> 领队马走向目标工位并显示派单气泡。
  - `tool_call_started` -> 目标小马 working/tool_running。
  - `tool_call_finished` -> 小马点头或更新进度。
  - `task_completed` -> 成果传递。
  - `report_ready` -> 报告钉上白板。
  - `approval_required` -> 目标小马暂停工作，UI 弹出审批；允许/拒绝后恢复或失败。
  - `task_failed` -> 挠头道歉。
- [ ] 窗口尺寸：默认 1440 x 900，最小 1280 x 800；低于 1280 宽时日志默认折叠并提示建议放大窗口。

## 4. 架构与模块边界

- [ ] 保持 main/preload/renderer/shared 分层：main 访问高权限能力，preload 暴露最小白名单 API，renderer 只做 UI。
- [ ] BrowserWindow 安全配置必须包含 `contextIsolation: true`、`nodeIntegration: false`、`webSecurity: true`；优先评估 `sandbox: true`。
- [ ] 不引入传统 Web 后端；主进程就是本地可信执行边界。
- [ ] LLM/API Key 只在主进程或安全配置层读取，renderer 不得知道 key；日志中脱敏 Authorization、KEY、TOKEN、SECRET、PASSWORD。
- [ ] SQLite 只在主进程访问；上传 xlsx 与系统表隔离，SQL 查询使用只读校验和结果行数上限。
- [ ] MCP server 由主进程 stdio 托管；renderer 不直接启动、停止或调用 MCP。
- [ ] filesystem MCP 和文件马能力必须限制在 workspace root 内，路径需 canonicalize 后校验，禁止 `../` 和符号链接逃逸。
- [x] 建立安全审批与权限治理中心作为独立可信边界：策略判断、审批请求生成、token 校验和真实执行必须在 main；renderer 只展示审批 UI 并回传用户决策。
- [x] 新增 `ApprovalRequest`、`PermissionPolicy`、`AuditLogEntry` 等共享类型时，同步检查 `TaskLog`、store、preload、main handler 和文档。
- [ ] 报告生成建议逐步收敛为 `ReportSpec -> schema 校验 -> 固定 HTML 模板 -> ECharts`，减少 LLM 直接输出任意 HTML 的风险。
- [ ] Agent 事件流作为动画和日志唯一事实源；不要在 renderer 伪造任务状态。
- [ ] 数据与演示汇总：各 §2 的数据来源和演示策略已对齐；fixture 集中在 `assets/demo/`、`src/renderer/src/mock/` 或未来 `src/fixtures/`；禁止 JSX 内散落 mock。
- [ ] Electron dev 刷新要求：确认 `npm run dev` 能支持 renderer HMR、preload 改动刷新、main 改动重启；必要时改为 `electron-vite dev --watch` 或配置 `build.watch`。

## 5. 设计系统

- [ ] 使用 [design-system/小马办公室/MASTER.md](design-system/小马办公室/MASTER.md) 作为 UI 技术栈、图标、占位、数据演示策略和视觉 token 合同。
- [ ] 设计风格保持“有机极简主义 x 高级室内设计”：亚麻白、燕麦、驼色、赭石、鼠尾草绿、赤陶、黄铜金。
- [ ] 当前 `src/renderer/src/theme.css` 已有基础 token，后续改动优先增补变量，不在组件里散落硬编码颜色。
- [ ] React UI 图标统一规划为 `lucide-react`；安装和替换图标时禁止混用多套图标库，禁止用 emoji 充当功能图标。
- [ ] 小马、办公室、配件优先用 Pixi Graphics 程序化矢量；远程图库默认关闭，`IMAGE_PROVIDER=none`。
- [ ] 报告空态、白板空态、示例数据入口必须明确状态，不得用预制成功报告冒充真实分析。
- [ ] 覆盖关键界面的默认态、空态、加载态、成功态、错误态、未配置态。

## 6. API 与集成

- [ ] LLM provider 使用 OpenAI-compatible 配置，支持 DeepSeek / 通义 / Kimi / 自定义 baseURL；模型连接测试显示具体失败原因。
- [ ] API key 开发期可来自 `.env`，后续桌面用户配置优先接系统凭据存储；SQLite 中只存 key 引用或配置状态，不存明文。
- [ ] Vercel AI SDK tool-calling：领队马使用 `dispatch`，职能小马使用各自 system prompt + Skill + 工具集。
- [ ] LLM 调用设置超时、重试、流式输出和错误标准化；超时/429/5xx 可重试，重试耗尽后真实失败。
- [ ] SheetJS/SQLite：xlsx 解析、schema/sampleRows、只读 SQL、错误回喂重试形成闭环。
- [ ] MCP：配置 `name/command/args/env/enabled`，主进程 stdio 托管、工具发现、状态展示、超时和崩溃处理。
- [x] 工具风控：所有 MCP/tool call 执行前统一经过风险分类、权限策略校验和必要审批；拒绝结果以工具失败返回给 Agent。
- [ ] Skill script：默认需审批；脚本参数、工作目录、可访问文件和环境变量必须受控，日志脱敏。
- [ ] 审批 token：main 生成短期一次性 token，绑定审批请求和资源目标；renderer 不获得长期权限。
- [ ] ECharts/报告：报告图表使用本地资源，禁止 CDN 和 LLM 任意脚本；图表失败时降级为文本/表格报告。
- [ ] PDF：使用 Electron `printToPDF`，等待 ECharts 渲染后导出；失败时保留 HTML 报告并提示重试。
- [ ] 邮件：文书马只生成 `email_draft` 草稿，不接 SMTP、真实邮件 API 或后台静默发送。
- [ ] 日志脱敏：工具入参、LLM 请求摘要、MCP env、用户上传数据日志都需要脱敏和长度限制。

## 7. 平台能力与权限

- [ ] 主进程负责窗口、SQLite、文件系统、MCP 子进程、LLM、PDF；renderer 只能通过 preload 白名单 API 调用。
- [ ] 默认工作区建议为应用数据目录下 `PonyOfficeWorkspace`，允许用户选择；实际路径存配置，代码中不硬编码环境路径。
- [ ] 文件上传先进入受控 staging/imports，再解析入 SQLite；解析失败不写半成品数据。
- [ ] 归档目标必须在工作区内；覆盖、删除、移动、取消任务、启动带写权限 MCP server 需人类确认。
- [x] 确认流程建议由 main 发起 `approval_required`，renderer 弹窗确认后返回一次性 token，main 校验动作和目标后执行。
- [x] 权限策略默认保守：自定义小马默认只读；绑定 MCP 或 Skill script 时必须显式选择策略等级。
- [x] 审计日志存 SQLite，保留高风险拦截、审批决策和最终执行结果；敏感参数只存脱敏摘要。
- [ ] 应用退出前关闭 MCP 子进程、SQLite、临时 PDF/HTML；任务运行中关闭窗口需确认。
- [ ] Windows 专项：文件占用、路径过长、MCP 子进程被拦截时给出可恢复提示。
- [ ] macOS/Linux 专项：目录权限、PDF/字体依赖、文件管理器打开失败均需要降级说明。

## 8. 验证

- [ ] 运行 `npm run typecheck`，确保主进程、preload、renderer 类型边界可通过。
- [ ] 增加或补齐测试脚本后运行：`npm run test`、`npm run test:agent`、`npm run test:e2e -- --grep "core demo"`、`npm run test:pdf`、`npm run test:mcp-filesystem`。
- [ ] Electron 安全验证：renderer 中无法访问 `fs`、`process`、`child_process`、原始 `ipcRenderer` 或 `.env`。
- [ ] Pixi 视觉验证：canvas 非空、3 秒内有帧变化、1280 x 800 下无关键遮挡。
- [ ] xlsx 验证：演示电信数据可导入；损坏文件、空表、非 xlsx 被拒绝。
- [ ] SQL 验证：只允许 SELECT；危险 SQL、多语句、DDL/DML 被拒绝；失败重试最多 2 次。
- [ ] Agent 验证：典型任务产生 `task_dispatched`、`tool_call_started`、`tool_call_finished`、`task_completed` 或 `task_failed`。
- [ ] 报告验证：报告含真实分析结论、至少 1 个图表、可打开、可导出 PDF。
- [ ] MCP 验证：filesystem MCP 只能访问工作区；越界路径、符号链接逃逸、删除/覆盖取消均处理正确。
- [ ] 审批治理验证：覆盖/删除/移动、Skill script 执行、自定义小马调用 MCP 均按权限策略进入允许、拒绝或直接拦截路径。
- [ ] 一次性 token 验证：重复使用、过期使用、目标篡改、renderer 伪造 token 均失败。
- [ ] 审计日志验证：审批、拒绝、直接拦截、执行成功/失败均有记录且脱敏。
- [ ] 失败演示：断网/API 超时、SQL 错误、MCP 失败时应用不崩溃，日志和道歉动画一致。
- [ ] 生成或更新 `TESTING_CHECKLIST.md` 与 `test-results/` 证据。
- [ ] 列出失败/跳过项与下一步建议，禁止把未验证项标为完成。

## 9. 开发完成后更新 PRD

- [ ] 调用 update-prd Skill。
- [ ] 基于实际代码创建或更新 `prd/` 文档体系。
- [ ] 对照原始 `prd.md` 总目标与 `prd/` 已实现文档，把未完成差距写入本 TODO。
- [ ] 将根目录 `prd.md` 归档为 `prd.original.md`（保留原始总目标；Agent 后续不读取，仅供人类回顾）。
- [ ] 生成或更新同内容的 `CLAUDE.md` 和 `AGENTS.md`，提示后续基于 `TODO.md` 继续任务。

## 10. 未完成目标 / 后续功能

- [ ] 多办公室、云端同步、用户登录、多会话标签页。
- [ ] 复杂 DAG 可视化编辑器、长期任务调度、多领队模式。
- [ ] csv/ods/超大文件流式导入、用户手动字段映射 UI。
- [ ] 报告拖拽式 BI、多页仪表盘、在线分享。
- [ ] 插件市场、远程 MCP 托管、Secret 云同步、Skill 版本管理。
- [ ] 多用户审批流、审批人角色、远程审计平台、复杂策略 DSL、长期授权。
- [ ] SMTP 或真实邮件 API 发送。
- [ ] Word/PPT 自动排版、多版本审阅流。
- [ ] 自动切换多模型供应商、云端错误上报。

## 开发进度

> 每轮 `project-iterate` 或开发 session 结束前追加一条；规范见 `.claude/rules/todo-writeback.md`。

- 2026-06-11：由 `02-project-prepare` 基于 `prd.md`、现有项目结构和多 Agent 拆解生成首版 `TODO.md` 与 `design-system/小马办公室/MASTER.md`；尚未执行开发验收，所有功能项保持未勾选。

## TODO 卸货记录

> 每次将已完成块从 §2 归档到 `prd/` 时追加；规范见 `.claude/rules/todo-prd-archive.md`。

（暂无）
