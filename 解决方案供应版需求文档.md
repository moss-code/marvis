# 需求文档 PRD

## 一、产品名称

**翼智小马：数字员工解决方案整合供应平台**

## 二、产品概述

翼智小马是一款桌面端数字员工解决方案管理与运行工具。平台统一接入数字员工、MCP工具、知识库和模型服务，通过可视化方式编排解决方案，并由统一调度模块自动指派数字员工完成任务。

产品首先服务集团内部，减少重复人工工作；成熟方案可打包授权给企业客户，并组合运营商云算力、网络流量、模型 Token、网络安全和数据安全服务形成 To B 产品。

## 三、产品目标

- 统一接入和管理不同来源的数字员工与工具。
- 将多个数字员工组合成可运行、可发布的解决方案。
- 根据任务自动派单并展示数字员工协作过程。
- 支持方案授权、资源用量统计和基础计费。
- 保障租户、数据、模型和工具调用安全。
- 完成经营分析、智能营销、调账稽核三个首批方案。

## 四、目标用户

| 用户 | 使用目标 |
| --- | --- |
| 内部业务人员 | 选择解决方案或直接发起任务，获得自动执行结果 |
| 解决方案经理 | 组合数字员工和工具，发布解决方案包 |
| 数字员工开发者 | 接入、测试和维护数字员工能力 |
| 平台运营人员 | 查看运行状态、效果、成本和异常 |
| 安全管理员 | 管理数据、工具、模型权限和审计策略 |
| 企业客户管理员 | 管理本企业用户、授权、资源配额和使用情况 |

## 五、技术栈

| 层 | 选型 |
| --- | --- |
| 应用壳 | Electron + Vite + React + TypeScript |
| 场景渲染 | PixiJS v8 |
| Agent 编排 | Vercel AI SDK（tool-calling 循环 + 流式输出）+ 自写派单薄层 |
| LLM | 国内模型（DeepSeek / 通义 / Kimi 等）走 OpenAI 兼容接口，可配置切换 |
| MCP | `@modelcontextprotocol/sdk`（官方 TS SDK），stdio 子进程托管 |
| 数据库 | SQLite（better-sqlite3） |
| 表格解析 | SheetJS（xlsx） |
| 图表 | ECharts |

### 技术边界

- MVP 采用 Electron 桌面应用，优先满足内部试点和比赛演示。
- React 构建管理界面，PixiJS 展示数字员工角色、协作连线和实时状态。
- Vercel AI SDK 负责模型流式输出及工具调用循环；自写派单薄层仅负责能力匹配、执行顺序、状态和人工接管。
- MCP Server 作为本地 stdio 子进程运行，接入数据库、EOP、企业微信等工具。
- SQLite 保存配置、方案、任务、日志和用量；企业级多租户服务端在商业化阶段扩展。

## 六、核心业务对象

| 对象 | 说明 |
| --- | --- |
| 数字员工 | 具备明确职责、工具权限和输入输出的 Agent |
| MCP工具 | 数据查询、EOP、消息推送等可调用工具 |
| 解决方案 | 多个数字员工和工具组成的任务流程 |
| 方案包 | 解决方案及其配置、权限、资源和授权信息 |
| 任务 | 用户或业务系统发起的工作要求 |
| 执行步骤 | 数字员工执行的子任务、状态和结果 |
| 租户授权 | 企业可使用的方案、功能、期限和配额 |
| 资源用量 | Token、任务次数、运行时长和网络流量 |

## 七、模块功能说明

### 1. 首页与任务入口

- 提供自然语言任务输入框。
- 支持上传会议文本和 Excel 数据。
- 提供经营分析、智能营销、调账稽核快捷入口。
- 展示最近任务、待确认事项和运行结果。

### 2. 数字员工接入中心

- 创建数字员工并填写名称、职责、提示词和版本。
- 配置可用模型、MCP工具和输入输出字段。
- 支持连接测试、模拟任务和启停控制。
- 记录提供方、风险等级、资源消耗和调用指标。
- 对第三方数字员工执行接入审核。

### 3. MCP工具管理

- 使用官方 TS SDK 启动和托管 stdio MCP 子进程。
- 展示 MCP Server、工具列表和参数结构。
- 配置启动命令、环境变量、超时和权限。
- 支持连通性测试、工具试调和错误日志。
- 首期接入数据查询、EOP调账和企业微信工具。

### 4. 能力资产目录

- 按业务场景展示数字员工和MCP工具。
- 支持搜索、分类、标签和状态筛选。
- 展示能力说明、版本、依赖、成功率和成本。
- 支持将能力加入解决方案编排画布。

### 5. 解决方案编排中心

- 使用可视化节点编排数字员工、工具、条件和人工确认。
- 支持节点连接、顺序执行、并行分支和条件判断。
- 支持参数映射和上下文传递。
- 支持测试运行、日志查看和失败定位。
- 支持保存草稿、发布版本、复制和回滚。

### 6. 统一派单与Agent执行

- 识别任务意图、业务类型和关键参数。
- 根据数字员工专长、权限、状态和成本进行派单。
- 通过 Vercel AI SDK 运行流式 tool-calling 循环。
- 支持串行执行、失败重试、超时和停止。
- 对高风险或低置信度任务请求人工确认。
- 保存完整任务上下文和执行结果。

### 7. 数字员工协作场景

- 使用 PixiJS v8 展示数字员工角色和工作区域。
- 实时展示待命、分析、调用工具、协作、待确认、完成和异常状态。
- 展示数字员工之间的任务流转和数据传递。
- 点击数字员工可查看当前子任务、工具调用和依据。
- 动画只表达真实执行状态，不替代日志信息。

### 8. 方案产品与授权

- 将数字员工、MCP工具、流程和配置打包为方案。
- 配置方案名称、场景、版本和交付说明。
- 配置可用期限、任务配额和Token配额。
- 支持内部授权、客户试用、正式开通和停用。
- 展示方案使用次数、客户和版本分布。

### 9. 资源用量与基础计费

- 统计模型输入、输出和总Token。
- 统计任务次数、执行时长和MCP调用次数。
- 记录云算力、网络流量和安全服务的配置项。
- 支持设置配额、预算和超量告警。
- 根据授权费、方案费和资源用量生成费用预估。
- MVP只提供计量和费用预估，不实现完整财务结算。

### 10. 安全与隐私

- 对数字员工、模型、工具和方案进行权限控制。
- 敏感字段进入模型前进行过滤或脱敏。
- MCP工具使用白名单和参数校验。
- 高风险流程操作必须经过规则授权或人工确认。
- 保存模型、工具、数据和人工操作日志。
- 支持本地存储、数据不出域和日志清理策略。

### 11. 运行监控与审计

- 展示任务量、成功率、平均耗时和人工接管率。
- 展示各数字员工和工具的调用情况。
- 统计Token、任务和资源成本。
- 查询执行步骤、模型输出、工具参数和错误信息。
- 使用 ECharts 展示趋势、排行和方案效果。

### 12. 数据导入与表格处理

- 使用 SheetJS 导入 Excel 数据字典、用户数据和配置模板。
- 提供字段预览、列映射和格式校验。
- 支持将处理结果导出为 Excel。
- 导入数据默认保存在本地 SQLite，敏感信息可选择不持久化。

### 13. 经营分析解决方案

- 根据自然语言识别指标、时间和筛选条件。
- 调用数据知识和查询工具生成受控 SQL。
- 校验只读权限、敏感字段和查询范围。
- 返回表格、ECharts图表和分析结论。

### 14. 智能营销解决方案

- 导入或查询客户数据并生成用户画像。
- 识别客户需求、痛点、价值和风险。
- 根据人员专长和负载完成匹配。
- 生成沟通话术、派单理由和跟进任务。

### 15. 调账稽核解决方案

- 通过 EOP MCP 工具获取调账单内容。
- 核验校园赠送金工单的调账类型和备注。
- 一致时记录通过并允许流程继续。
- 不一致时生成原因并自动退回申请人。
- 保存规则版本、稽核结果和流程日志。
- 后续扩展附件 OCR 和多模态一致性检查。

## 八、核心交互流程

```text
用户创建任务
  -> 派单薄层识别任务类型和参数
  -> 选择解决方案与数字员工
  -> Vercel AI SDK运行流式工具调用
  -> MCP工具返回业务数据或执行结果
  -> PixiJS同步展示数字员工状态
  -> 结果质检或请求人工确认
  -> 展示、导出或推送结果
  -> 记录任务、Token、工具和资源用量
```

## 九、本地数据设计

| 表 | 主要内容 |
| --- | --- |
| digital_employee | 数字员工信息、提示词、版本和权限 |
| mcp_server | MCP启动配置、状态和环境变量引用 |
| capability | 数字员工与工具能力目录 |
| solution | 解决方案基本信息和发布状态 |
| solution_node | 编排节点、连接、参数和执行顺序 |
| task | 任务来源、内容、状态、风险和结果 |
| task_step | 数字员工步骤、工具调用和状态 |
| tool_call_log | MCP工具参数、结果、耗时和错误 |
| model_usage | 模型、Token、耗时和估算费用 |
| authorization | 方案授权、期限和配额 |
| security_audit | 数据访问、人工确认和高风险操作 |
| app_setting | 模型、界面、安全和本地配置 |

## 十、非功能需求

### 1. 性能

- 应用启动后主界面应在5秒内可操作。
- 模型输出采用流式展示。
- PixiJS场景在普通办公电脑上保持流畅。
- SQLite操作不得阻塞渲染线程。

### 2. 稳定性

- MCP子进程异常退出时给出提示并支持重启。
- 任务支持停止、重试和恢复。
- 模型或工具不可用时允许切换或转人工。

### 3. 安全

- API Key、MCP凭据不得明文展示。
- Electron开启上下文隔离，限制渲染进程权限。
- 模型和工具调用执行最小权限控制。
- 敏感数据默认脱敏并记录访问审计。

### 4. 可扩展性

- 新模型通过OpenAI兼容配置接入。
- 新工具通过MCP Server注册接入。
- 新数字员工无需修改调度核心即可注册。
- 新解决方案通过编排和配置形成。

## 十一、MVP范围

- Electron桌面应用和React管理界面。
- PixiJS数字员工协作场景。
- 数字员工及MCP工具接入。
- 基础方案编排、发布和运行。
- 自写派单薄层与Vercel AI SDK执行循环。
- SQLite本地配置、任务、日志和用量存储。
- 基础授权、Token计量、安全审计和人工接管。
- 经营分析、智能营销、调账稽核三个演示方案。

## 十二、验收标准

| 验收项 | 标准 |
| --- | --- |
| 数字员工接入 | 可创建数字员工并配置模型、工具和提示词 |
| MCP接入 | 可托管stdio MCP子进程并完成工具调用 |
| 方案编排 | 可组合多个数字员工和工具并保存发布版本 |
| 自动派单 | 可根据三类任务选择相应数字员工和方案 |
| 流式执行 | 模型和工具执行过程实时显示且可停止 |
| 场景可视化 | PixiJS能够同步展示数字员工状态和协作关系 |
| 经营分析 | 能完成一次自然语言取数和图表分析流程 |
| 智能营销 | 能完成一次客户画像、人员匹配和话术生成流程 |
| 调账稽核 | 能获取调账单并完成一致性判断和流程处理 |
| 用量统计 | 能记录任务、Token和工具调用用量 |
| 安全审计 | 高风险操作和数据访问可以查询追溯 |

## 十三、后续规划

- 将本地 SQLite 数据迁移或同步到企业服务端，支持正式多租户。
- 增加完整授权订购、账单结算和客户交付能力。
- 接入运营商云、专线、流量、Token和安全产品接口。
- 扩展客服、网络告警、装维、方案报价等解决方案。
- 开放第三方数字员工接入，形成解决方案供应生态。
